<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:imageButtonStyle">@drawable/ic_filter_state</item>
    </style>

    <style name="MyImageButton" parent="android:Widget.ImageButton">
        <item name="android:background">@drawable/ic_filter_state</item>
    </style>


    <style name="MyButton" parent="android:Widget.Material.Button">
        <item name="android:background">@drawable/ic_filter_state</item>
    </style>


</resources>
